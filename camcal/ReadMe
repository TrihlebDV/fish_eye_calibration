Описание:
	в данном разделе проводится вычисление искривления(calibration) и после его устранение и подгонка подсчетов. Полученные результаты записываются в файле формата YAML для его дальнейшего чтения в программах для роботехнического комплекса.
Подразделы:
	(calibration): вычисление кривизны линзы через шахматную доску определенного размера(chessboard XxY) и запись полученных результатов.
	(camcal_robot): необходимые программы для получения кадров с камеры для каллибровки.
	(NextStep): окончательная подгонка и получение готовых данный дисторсии и искривления для их дальнейшего использования(не дописанно 26.09.2018)
	(sceletization): скелетизация, тестовая программа
	(YAML): пример использования записи и считывания YAML файлов

